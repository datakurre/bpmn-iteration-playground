[
  {
    "$schema": "https://unpkg.com/@camunda/zeebe-element-templates-json-schema/resources/schema.json",
    "name": "Pi Agent Turn",
    "id": "graph-agent.pi-agent-turn",
    "version": 1,
    "description": "One LLM turn, run by Pi. The BPMN token moves once per turn.",
    "category": { "id": "graph-agent.pi-agent", "name": "Pi Agent" },
    "appliesTo": ["bpmn:ServiceTask"],
    "elementType": { "value": "bpmn:ServiceTask" },
    "properties": [
      {
        "type": "Hidden",
        "value": "agent:turn",
        "binding": { "type": "zeebe:taskDefinition", "property": "type" }
      },
      {
        "label": "Agent role",
        "description": "Canned instructions layered in front of this turn's prompt. Leave blank for an ordinary turn; 'graph_architect' is the only role defined today (src/agent/harnesses.ts's AGENT_ROLES).",
        "type": "String",
        "value": "",
        "binding": { "type": "zeebe:taskHeader", "key": "agent_role" }
      },
      {
        "label": "Instructions",
        "description": "FEEL expression for the free-form instructions this turn receives.",
        "type": "String",
        "feel": "required",
        "value": "=intent",
        "binding": { "type": "zeebe:input", "name": "prompt" }
      },
      {
        "label": "Stop reason",
        "description": "Process variable the turn's stop reason is published to.",
        "type": "String",
        "value": "stop_reason",
        "binding": { "type": "zeebe:output", "source": "=stop_reason" }
      },
      {
        "label": "Summary",
        "description": "Process variable the turn's summary is published to.",
        "type": "String",
        "value": "summary",
        "binding": { "type": "zeebe:output", "source": "=summary" }
      }
    ]
  }
]
