# Devenv & Nix
.devenv*
devenv.local.nix
devenv.local.yaml
.direnv/
.venv/

# Pre-commit & Tooling
.pre-commit-config.yaml

# Environment & Data
.env
.env.local
# Root-anchored: graph_agent/data/ is tracked package data (pi-demo, workspace_templates),
# not the ZODB storage directory this is meant to ignore.
/data/
*.db
*.zodb
# This repo's own checkout is also a valid `graph-agent` workspace (running `graph-agent serve` or the
# tests from here creates .agents/ at the repo root the same way it would in any project).
/.agents/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
coverage.xml
coverage/

# Node
node_modules/
graph_agent/static/bpmn-modeler-bundle.js
graph_agent/static/bpmn-viewer-bundle.js
graph_agent/static/pages/
graph_agent/static/tailwind.css
graph_agent/static/diagram-js-minimap.css
graph_agent/static/properties-panel.css
graph_agent/static/bpmn-js-token-simulation.css
graph_agent/static/bpmn-js-bpmnlint.css
graph_agent/static/element-template-chooser.css

# Temporary & Verification Outputs
*.log
scratch/
plans/
todos/
.mypy_cache/
site/
result
result-*
TODO.md
