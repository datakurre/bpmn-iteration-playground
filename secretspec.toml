[project]
name = "bpmn-ai-starter"
revision = "1.0"

[defaults]
provider = "env"

[providers]
env = "env://"

[profiles.default]
OPENCODE_API_KEY = { description = "OpenCode API key (Zen and Go share one route)", required = false }

[scopes.opencode]
secrets = ["OPENCODE_API_KEY"]
