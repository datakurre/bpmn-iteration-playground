[
  {
    "$schema": "https://unpkg.com/@camunda/element-templates-json-schema/resources/schema.json",
    "name": "Graph Extend Task",
    "id": "playground.graph-extend-task",
    "version": 1,
    "description": "Service task that applies an extension_spec to extend the running workflow.",
    "appliesTo": ["bpmn:ServiceTask"],
    "properties": [
      {
        "label": "Harness type",
        "type": "Hidden",
        "value": "graph_extend",
        "editable": false,
        "binding": { "type": "camunda:property", "name": "harness_type" }
      },
      {
        "label": "Extension Spec (input)",
        "description": "Process variable containing the ExtendRequest JSON to apply.",
        "type": "String",
        "value": "${extension_spec}",
        "editable": true,
        "binding": { "type": "camunda:inputParameter", "name": "extension_spec" }
      },
      {
        "label": "Migrate Status (output)",
        "description": "Process variable to publish the extension result status to.",
        "type": "String",
        "value": "migrate_status",
        "editable": true,
        "binding": { "type": "camunda:outputParameter", "source": "${status}" }
      }
    ]
  }
]
